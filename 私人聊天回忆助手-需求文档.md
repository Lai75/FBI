# 私人聊天回忆助手 — 敏捷产品需求文档

**版本：** v1.0（MVP）  
**暂定名称：** 聊回忆  
**一句话定位：** 帮用户从自己的聊天记录中，快速找回人与事的来龙去脉。  
**首发用户：** 有大量长期私聊/群聊记录、经常需要回忆共同朋友和往事的个人用户。

## Problem Statement

用户和朋友聊天时，经常会提及某位共同朋友或以前发生的事情，例如“上次小丽搬家时到底发生了什么？”“我们什么时候谈过那次误会？”用户通常只记得人物、模糊时间或零碎关键词，不记得聊天发生在哪个应用、哪个窗口或哪一天。

现有聊天软件主要按会话窗口和关键词组织内容；用户必须逐个翻找，难以跨对话将一个人、一件事的上下文串起来。普通笔记工具也无法从原始聊天中回到具体原话。用户需要一个只属于自己的私密空间，将自愿上传的聊天内容组织为可检索、可验证的“人物—事件—时间线”。

关键问题不是“怎样保存聊天”，而是：**怎样在不篡改记忆、不泄露隐私的前提下，在几十秒内找回某个人或事件的背景与原话。**

## Solution

用户粘贴或上传自己有权保存的聊天记录后，系统提取消息的时间、会话对象与正文，并让用户在保存前核对。用户可通过人物、事件、时间范围或关键词搜索。系统返回“事件卡片”：说明相关时间、涉及人物、简短经过、相关聊天片段，并提供一键回到原始上下文的入口。

事件卡片是**可编辑的提示，不是事实裁决**。系统必须保留每项总结的原文来源；用户可以修正人物、修改标题、合并或拆分事件、删除误判。只有用户本人可访问自己的资料。

### 核心使用流程

1. 用户想起线索，例如“我和阿华之前聊过小丽搬家”。
2. 用户搜索“小丽搬家”，或先输入“小丽”再限定大约日期。
3. 系统先从该用户自己的资料中检索相关记录，再按照相关度返回事件和消息片段。
4. 用户打开“2026 年 3 月：小丽搬家讨论”事件卡片，阅读简短经过和涉及人物。
5. 用户展开原始消息，确认具体说法、时间和聊天对象。
6. 如卡片把不同事情混在一起，用户可修改、拆分或标记“不相关”；该反馈仅影响自己的资料。

## Product Goals

### MVP 目标

- 用户能以人物、关键词和日期三个维度找回自己的历史对话。
- 用户能在一个事件视图中理解基本脉络，并能查看原话确认。
- 系统对不确定的人物与事件明确标示，而非假装准确。
- 用户始终掌握数据：可查看、编辑、导出和删除。

### 成功指标（Beta 上线后 8 周）

| 指标 | 目标 |
| --- | --- |
| 用户完成首次导入并成功保存的比例 | ≥ 70% |
| 用户在搜索后打开至少一条结果的比例 | ≥ 75% |
| 从输入查询到打开原始相关消息的中位耗时 | ≤ 45 秒 |
| 用户对事件卡片标记为“有帮助”的比例 | ≥ 65% |
| 事件卡片被用户修改/拆分的比例 | 记录为质量指标，不设越低越好目标 |
| 隐私或越权访问事故 | 0 |

### 不在 MVP 范围

- 替代即时通讯、发送消息或读取用户设备中的聊天应用。
- 自动抓取未经用户主动导出的聊天内容。
- 公开分享、社交网络、陌生人匹配或多人共同资料库。
- 全平台导入；只支持经过验证的少数格式。
- 用 AI 判断谁对谁错、推断真实意图、给人贴人格或关系标签。

## User Stories

1. 作为用户，我想创建私密账户，以便只有我能查看我上传的聊天内容。
2. 作为用户，我想在上传前知道系统如何处理我的聊天内容，以便决定是否愿意提交。
3. 作为用户，我想粘贴少量聊天文本，以便快速保存一段我在意的对话。
4. 作为用户，我想上传受支持的聊天导出文件，以便不用逐条复制长期对话。
5. 作为用户，我想在保存前预览系统识别的日期、发送者和正文，以便纠正解析错误。
6. 作为用户，我想给一次导入命名，以便日后知道它来自哪个聊天或时期。
7. 作为用户，我想搜索某人的名字，以便找到和他/她有关的历史对话。
8. 作为用户，我想搜一个事件线索或关键词，以便找回我只记得片段的往事。
9. 作为用户，我想按大概日期筛选，以便减少同名或重复话题造成的干扰。
10. 作为用户，我想看到相关聊天片段及其时间，以便判断结果是否是我要找的内容。
11. 作为用户，我想查看一个事件的简短脉络，以便不必先阅读几百条原始消息。
12. 作为用户，我想知道事件总结来自哪些原话，以便验证它是否准确。
13. 作为用户，我想展开前后上下文，以便避免一句话被断章取义。
14. 作为用户，我想看到系统推定涉及的人物，以便快速理解谁参与了这件事。
15. 作为用户，我想修正人物名称或别名，以便避免系统把同名或不同称呼的人混淆。
16. 作为用户，我想修改事件标题和摘要，以便让内容符合我的实际记忆。
17. 作为用户，我想将错误合并的事件拆分，以便不同事情不会混在一起。
18. 作为用户，我想合并明显是同一件事的片段，以便完整查看它的发展。
19. 作为用户，我想标记某条结果“不相关”，以便之后的检索更准确。
20. 作为用户，我想浏览按时间排列的事件，以便回顾某段时期发生过的事。
21. 作为用户，我想随时删除单次导入、某个事件或整个账户，以便重新掌控敏感数据。
22. 作为用户，我想导出我的记录和原文，以便不被某个服务锁定。
23. 作为用户，我想在解析或上传失败时看到明确原因，以便知道如何修复或改用粘贴方式。
24. 作为用户，我想关闭自动事件整理，以便只用原文搜索和手动标签。
25. 作为用户，我想知道哪些内容会发送给 AI 处理，以便为每次处理作出知情同意。

## Functional Requirements

### 1. 账户、授权与同意

- 提供注册、登录、密码重设和安全登出。
- 所有记录以用户为唯一数据所有者；任何读取、编辑、导出、删除请求都必须先验证所属关系。
- 首次上传和首次使用自动整理前，明确告知：用户应仅上传自己有权保存的内容；其中可能含第三方个人信息。
- 自动整理为独立同意项。用户可选择“只保存并搜索原文”。

**验收标准：** 未登录用户无法访问记录；修改 URL 或记录 ID 不能读取其他用户内容；关闭自动整理不会阻止手动保存与检索。

### 2. 导入与原始记录

- MVP 支持纯文本粘贴与 UTF-8 `.txt`/`.csv` 上传；文件上限暂定 10 MB。
- 导入流程：选择来源 → 校验格式/大小/编码 → 解析预览 → 用户修正 → 确认保存。
- 系统应尝试解析时间、发送者和正文；无法识别时原样保留并标为“待确认”。
- 用户能补充聊天标题、来源、日期范围和时区。
- 默认只长期保存结构化的原始消息，不长期保留上传文件二进制；是否保留源文件应由用户明确选择。

**验收标准：** 取消流程不产生可搜索记录；无效文件显示可理解的失败原因；预览中可修改日期、时区、发送者和标题。

### 3. 人物与别名

- 人物是用户资料库内的私有实体，不代表现实世界身份，也不跨用户关联。
- 系统可从消息发送者和正文中建议人物名称；用户确认后才建立或关联人物。
- 用户能设定别名，例如“小丽”“Lily”“丽姐”属于同一位人物，或将系统误关联的内容移除。
- 对低置信度识别应显示“可能是同一人”，不得自动合并。

**验收标准：** 用户可搜索人物及其别名；拆除关联后，该事件不再显示该人物；不同用户相同姓名绝不共享任何关系或资料。

### 4. 事件卡片与回溯

- 事件卡片字段：标题、时间范围、涉及人物、简短经过、相关消息、来源记录、用户修订状态。
- 自动生成内容必须为“建议草稿”，并列出支撑它的消息引用；无法确定时需明确写为“待确认”。
- 事件详情可从摘要跳转至原始消息，并显示至少相邻若干条上下文。
- 用户能编辑标题/摘要/人物，合并、拆分、删除或标记不相关。
- 仅用户确认的编辑才显示“已确认”；系统生成内容和用户编辑在界面上要可辨识。

**验收标准：** 每个自动摘要均能查到至少一条来源消息；用户能够在不依赖 AI 的情况下手动创建事件；事件删除后不再出现在事件浏览和搜索结果。

### 5. 搜索与浏览

- 搜索支持人物名称/别名、关键词、事件标题和原始消息正文。
- 支持按日期范围、来源会话和人物筛选；先按用户授权范围过滤，再进行内容搜索。
- 结果分为“事件”和“原始消息”两类，展示标题、日期、人物、匹配片段与相关度。
- 时间线按事件实际发生时间排序；日期不确定时显示“时间待确认”，不伪造精确日期。

**验收标准：** 输入人物或关键词可得到匹配内容；筛选可缩小范围；打开事件可回到原始消息；已删除内容不可被搜索。

### 6. 数据控制与安全

- 使用加密传输和加密存储；密钥、访问控制和备份策略应遵循最小权限原则。
- 应用日志、埋点、报错和客服工具不得收集消息正文、人物名字或事件摘要。
- 用户可导出自己的原始消息、人物、事件、标签与元数据。
- 用户可删除导入、事件或账户。MVP 建议 30 天可恢复的软删除，并在确认前明确说明恢复期限；到期后永久删除。
- 不得使用用户聊天内容训练模型或向其他用户展示，除非将来另有单独、清晰且可撤销的授权。

**验收标准：** 权限测试覆盖越权读写；日志抽查不含私人正文；删除记录后即时不可搜索，回收期届满后不可恢复。

## Implementation Decisions

### 领域模型与边界

以 **私人回忆库（Private Memory Library）** 作为核心领域边界和最高层测试接口。它负责整合导入、人物、事件、搜索及数据删除；其余模块都不能绕过它直接暴露用户内容。

| 实体 | 责任 | 关键字段 |
| --- | --- | --- |
| 用户 | 数据所有权与授权 | 用户 ID、认证、同意设置 |
| 导入批次 | 描述一次上传及处理进度 | 来源、状态、错误、时区 |
| 原始消息 | 保存可验证的对话依据 | 时间、发送者、正文、顺序、导入批次 |
| 人物 | 管理用户定义的称呼与别名 | 显示名、别名、确认状态 |
| 事件 | 组织与解释相关消息的用户私有视图 | 标题、时间范围、摘要、人物、状态 |
| 消息引用 | 连接事件结论与原话 | 消息 ID、上下文范围、引用用途 |

### 状态设计

- 导入批次：`已创建 → 校验中 → 待用户确认 → 保存中 → 已完成`；可进入`失败`或`已取消`。
- 事件：`草稿/系统建议 → 用户已确认 → 已删除`。
- 人物关联：`系统建议 → 用户已确认 / 已拒绝`。
- 只有“已完成”的导入内容可供检索；被删除内容在任何状态下均不可被搜索。

### 技术策略

- 解析器按来源格式独立实现，统一输出消息、时间、发送者和解析警告，避免单一格式的修改影响其他来源。
- 检索采用混合模式：精确关键词、日期过滤、人物别名匹配；语义搜索仅作为可选增强，不能替代原文可验证的精确搜索。
- 事件整理服务输入应尽量少：仅当前用户选择的候选消息和必要上下文；输出必须包含来源消息引用、不确定标记与可编辑草稿。
- AI 提供商需支持不将输入用于训练的商业数据处理条款；若做不到，自动整理不得上线。
- 所有写操作使用版本校验或等价机制，避免用户在多设备编辑时悄悄覆盖内容。

## Testing Decisions

好的测试验证用户结果：能否安全导入、找回正确内容、查看原话、修正错误以及彻底删除；不依赖内部数据库结构或某一个 AI 模型的固定措辞。

- **领域测试：** 验证私人回忆库的所有权、导入状态、事件—消息引用、人物别名、删除与恢复规则。
- **导入契约测试：** 使用有效文本、缺字段、乱码、日期格式混杂、超限文件与取消场景，验证统一解析输出与错误信息。
- **检索集成测试：** 验证关键词、人物别名、日期筛选、删除不可见和先授权后搜索。
- **事件生成测试：** 验证每项建议都含来源引用；无依据时显示待确认；用户修改、合并、拆分后的结果正确。
- **端到端测试：** 覆盖“上传 → 修正预览 → 保存 → 搜索人物/事件 → 查看上下文 → 编辑事件 → 删除”的完整流程。
- **安全与隐私测试：** 覆盖跨用户资源猜测、会话失效、导出范围、日志脱敏、AI 同意开关和删除后不可检索。
- **可用性测试：** 让目标用户带着一个真实但已获许可的回忆任务操作，观察是否能在 45 秒内回到可信原话。

## Agile Delivery Plan

### Sprint 0：问题验证与风险清除（1 周）

- 访谈 5–8 名目标用户，收集他们实际会找的“人物—事件—时间”线索。
- 验证 1–2 种最常见且合法的导出格式，准备已脱敏的测试样本。
- 完成隐私告知、删除策略、AI 数据处理要求及交互原型。
- 评估标准：用户能理解“事件是建议、原话是依据”的产品心智。

### Sprint 1：原文私密库（2 周）

- 认证、文本粘贴、导入预览/修正、原始消息保存、按关键词和日期检索。
- 用户即使不使用 AI，也能保存并找回一段聊天。

### Sprint 2：人物与可信回溯（2 周）

- 人物、别名、人物筛选、原始上下文展开、时间线。
- 完成用户隔离、导出、软删除和安全日志控制。

### Sprint 3：事件建议与纠错（2 周）

- 经明确同意的事件卡片生成、来源引用、待确认状态、手动编辑、合并/拆分和不相关反馈。
- 进行端到端、安全与可用性测试。

### Sprint 4：Beta 上线与学习（1–2 周）

- 小范围试用、埋点仪表盘、性能优化、无障碍修正。
- 根据“找回成功率”和“事件卡片有帮助率”决定下一步，而非根据上传数量盲目扩展格式。

## Risks and Mitigations

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 第三方隐私与地区法规 | 高 | 最小化收集、明确同意、仅用户私有访问、导出/删除；正式上线前获取目标市场的合规意见 |
| AI 错误串联人物或事件 | 高 | 把结果作为草稿、提供来源与上下文、确认/拆分/拒绝机制、保留原文优先级 |
| 导出格式复杂或变动 | 中 | 首版只支持验证过的格式；模块化解析器；粘贴文本始终作为兜底 |
| 用户不信任云端保存 | 高 | 清晰的数据流说明、加密、默认不保留源文件、可导出/删除；后续评估本地优先或端到端加密 |
| 搜索结果噪音 | 中 | 用人物别名、日期和来源会话作强筛选；展示原文片段而非只给摘要 |
| AI 与存储成本 | 中 | 文件限制、异步处理、按需生成事件、设置用量限额；非 AI 路径可独立成立 |

## Further Notes

### 产品原则

1. **原话优先于总结：** 摘要帮助回忆，原始上下文帮助确认。
2. **建议不等于事实：** 不确定内容必须显式标记，系统不能伪造确定性。
3. **隐私默认：** 不公开、不跨用户关联、不用作训练数据。
4. **用户拥有记忆：** 用户可以更正、带走和删除自己的资料。
5. **先找回，再聪明：** MVP 的第一价值是可靠搜索；AI 自动整理只能提升效率，不能成为产品可用的前提。

### 后续版本候选

- 更多经验证的聊天导出格式。
- 本地优先存储与端到端加密方案。
- 图片、语音、文件附件的 OCR/转写（需单独隐私评估）。
- 个人可控的“关系时间线”和“地点/事件回忆”，但不得生成未经依据的关系判断。
- 受用户明确授权的家庭/情侣共享回忆空间；这是独立产品线，需重新设计访问控制与撤销机制。
